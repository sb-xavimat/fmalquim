{
  "devDependencies": {
    "esbuild": "0.25.0"
  },
  "scripts": {
    "build": "esbuild lib/index.js --bundle --minify --target=es6 --outfile=out/rec0000_pgchem_helplib.min.js --charset=utf8 --global-name=chemHelpLib"
  }
}